# FOLDER STRUCTURE
```
.view/
    |-src -> core of react
        |- assets -> .png,.jpeg and etc, to help enchance the components
        |- components -> react components
            |- CreateNewQuiz.tsx -> Node ID: t1W47
            |- Footer -> .pen:Node ID: F92o8
            |- Header -> .pen :  Node ID: msK4q
            |- QuizThumb -> (DEPRECATED in design) 

        |- pages
            |- homeAdmin Node IDs: Gmi3x, mSFI3, NAKSU -> this sholdbe a spa the adminhomeview has does design
            |- newquizform.tsx -> Node ID: mnpa0
            |- quizdetail-> Node ID: C50pVv
```

# CURENT JOB

login screen, i need a login screen for the admin view for now dosen use real  auth just a place holder for future feature

user flow to acees the adm view in this development version:

enter(default guest home) -> clicl in login button -> open login page -> do to adm view home 